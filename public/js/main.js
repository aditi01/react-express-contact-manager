

ReactDOM.render(<ContactManager />, document.getElementById("component-container"));
// ReactDOM.render(<AddContact />, document.getElementById("modal-container"));

/*1. ReactDOM provides all the dom-specific methods. This renders the react element into the container and returns the 
reffereence to the component. This controls the content thatis displayed in the browser, by rendering the desired output 
into the reffered DOM*/